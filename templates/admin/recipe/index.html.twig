{% extends 'admin/admin.html.twig' %} 

{% block title %}Hello RecipeController!{% endblock %}

{% block body %}

<div class="example-wrapper">
    <h1>Hello ! ✅</h1>

    <a class="btn btn-primary btn-sm mb-2" href="{{ path('admin.recipe.create') }}"> Creer ta recette</a>

    <table class="table">
        <thead>
            <tr>
                <th>{{knp_pagination_sortable(recipes, 'ID', 'r.id')}}</th>
                <th>{{knp_pagination_sortable(recipes, 'Titre', 'title')}}</th>
                <th style="width: 200px">Actions</th>
            </tr>    
        </thead>
        {% for recipe in recipes %}
        <tr>
            <td>
                <a href="{{ path('admin.recipe.edit', {id: recipe.id}) }}"> {{ recipe.id}} </a>
            </td>
            <td>
                <a href="{{ path('admin.recipe.edit', {id: recipe.id}) }}"> {{ recipe.title}} </a>
            </td>
            
            <td>
                <div class="d-flex gap-1">
                    <a class="btn btn-primary btn-sm" href="{{ path('admin.recipe.edit', {id: recipe.id}) }}"> Editer </a>
                    <form action="{{ path('admin.recipe.delete', {id: recipe.id}) }}", method= "post" >   <input type="hidden" name="_method" value="DELETE">
                        <button type="submit" class="btn btn-danger btn-sm"> Supprimer </button>
            
                    </form>
                </div>

            </td>
        </tr>
        {% endfor %}
    </table>

    {{ knp_pagination_render(recipes)}}
</div>
{% endblock %}
